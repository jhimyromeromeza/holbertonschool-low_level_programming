#include <stdio.h>
/**
 * main - print numbre od base 10 starting from 0
 \*
 * Return: always 0 (succelss)
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
